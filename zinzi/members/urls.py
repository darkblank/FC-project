from django.conf.urls import url

urlpatterns = [
    # url(r'^/signup/$', ),
    # url(r'^/signin/$', ),
    # url(r'/(?P<user_pk>\d+)/profile/$', ),
    # url(r'/(?P<user_pk>\d+)/my-reservation/$' ),
]
